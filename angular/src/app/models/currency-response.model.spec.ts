import { CurrencyResponse } from './currency-response.model';

describe('CurrencyResponse', () => {
  it('should create an instance', () => {
    expect(new CurrencyResponse()).toBeTruthy();
  });
});
