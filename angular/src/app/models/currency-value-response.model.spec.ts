import { CurrencyValueResponse } from './currency-value-response.model';

describe('CurrencyValueResponse', () => {
  it('should create an instance', () => {
    expect(new CurrencyValueResponse()).toBeTruthy();
  });
});
